import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <header>test</header>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
